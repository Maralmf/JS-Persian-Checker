function validateNoneEnglish(rahsam) {
    var fieldValue = Xrm.Page.getAttribute(rahsam).getValue();
    if (!/^[\u0600-\u06FF\s]+$/.test(fieldValue)) {
        Xrm.Page.getAttribute(rahsam).setValue();
    }
}
